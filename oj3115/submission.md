# บันทึกการแก้โจทย์

ไฟล์นี้ต้องเขียนโดยนักศึกษาด้วยคำพูดของตนเอง

ใช้ template นี้เฉพาะกับโจทย์ OJ ที่ถูกระบุว่าต้องส่ง learning log เท่านั้น

ห้ามให้ AI เขียนไฟล์นี้แทนคุณ AI อาจช่วยตรวจ grammar, formatting หรือความชัดเจนได้ หลังจากที่คุณเขียนเนื้อหาของตนเองแล้ว

ถ้าใช้ AI กับโจทย์ learning-log-required นี้ ต้องทำไฟล์ `ai_reflection.md` ด้วย

---

## 1. ข้อมูล OJ

หมายเลข/ชื่อโจทย์ OJ:

```text
OJ3115 - [LEARNING LOGS] Arcade of Time: Store Check
```

OJ submission ID ถ้ามีการส่งแล้ว:

```text
631824
```

สถานะ OJ:

```text
Pass
```

เวลาที่ใช้คิดและทำโจทย์ด้วยตนเอง:

```text
30-60 minutes
```

เลือกหนึ่งข้อ:

```text
0-15 minutes
15-30 minutes
30-60 minutes
1-3 hours
3-6 hours
6-24 hours
1-3 days
4-7 days
1-4 weeks
More than 4 weeks
```

วิธีนับเวลา:

- นับเฉพาะเวลาที่ตั้งใจทำโจทย์นี้ด้วยตนเองจริง ๆ
- เริ่มนับตั้งแต่ตอนที่อ่านโจทย์ครั้งแรก
- ไม่นับเวลาพัก กินข้าว เรียน นอน เวลาที่ทำโจทย์อื่น หรือเวลาที่ไม่ได้ทำโจทย์นี้
- ถ้าใช้ AI ให้นับเฉพาะเวลาที่ทำด้วยตนเองก่อน prompt แรกที่ถาม AI
- ถ้าถามเพื่อน TA หรือผู้สอน ให้นับเฉพาะเวลาที่ทำด้วยตนเองก่อนขอความช่วยเหลือครั้งแรก
- ถ้าใช้ทั้ง AI และความช่วยเหลือจากคน ให้นับเฉพาะเวลาที่ทำด้วยตนเองก่อนขอความช่วยเหลือจากภายนอกครั้งแรก ไม่ว่าจะเป็น AI หรือคน
- ถ้าไม่ได้ใช้ AI และไม่ได้ขอความช่วยเหลือจากคน ให้นับเวลาถึงก่อนเขียน `submission.md`
- ประมาณเวลาได้ แต่ต้องซื่อสัตย์

---

## 2. ความเข้าใจโจทย์ของฉัน

เขียนโจทย์ด้วยคำพูดของตนเอง

ให้อธิบาย input, output และ constraints สำคัญด้วย

ถ้ายังไม่เข้าใจโจทย์ทั้งหมด ให้เขียนสิ่งที่เข้าใจในตอนนี้ ความเข้าใจอาจยังไม่ครบหรืออาจผิดได้ แต่ต้องพยายามอธิบายอย่างจริงใจ

```text
Input:
รับค่าจำนวนร้านค้าnumและจำนวนครั้งที่อยากเช็คเวลาcheckจากนั้นรับเวลาเปิด-ปิดของแต่ละร้านstart, stopและบรรทัดสุดท้ายรับเวลาที่ต้องการเช็คจริงๆ ตามจำนวน check ค่า
Output:
แสดงจำนวนร้านค้าที่กำลังเปิดอยู่ ณ เวลาที่เราเช็ค โดยให้พิมพ์ตัวเลขคำตอบเรียงต่อกันบรรทัดเดียวและคั่นด้วยช่องว่าง
Constraints:
ข้อมูลเวลาจะอยู่ในช่วง 0-1440 นาทีเที่ยงคืนถึงเที่ยงคืนอีกวันแต่จุดสำคัญคือจำนวนร้านและจำนวนครั้งที่เช็คมีมากถึง 100,000ซึ่งแปลว่าข้อนี้ข้อมูลเยอะมาก ถ้าเขียนโปรแกรมแบบวนลูปเช็คทีละร้านโค้ดจะรันช้าและติด Time Out แน่นอนต้องหาวิธีจัดการเวลาให้ดีกว่านี้
```

---

## 3. แผนแรกของฉัน

เขียนแผนแรกก่อนรับความช่วยเหลือจาก AI เพื่อน TA ผู้สอน หรือก่อนสรุป code สุดท้าย

ถ้าใช้ AI ให้เขียนแผนที่มีอยู่ก่อน prompt แรกที่ถาม AI

ถ้าถามเพื่อน TA หรือผู้สอน ให้เขียนแผนที่มีอยู่ก่อนถาม

ถ้าไม่ได้ใช้ AI และไม่ได้ขอความช่วยเหลือจากคน ให้เขียนแผนที่มีอยู่ก่อนหรือระหว่างเริ่มเขียน code

แผนนี้เขียนแบบคร่าว ๆ ได้ อาจยังไม่สมบูรณ์หรืออาจต่างจากวิธีสุดท้าย

สามารถเขียนเป็น pseudocode, flowchart idea หรือขั้นตอนความคิดได้

```text
Step1: รับค่าnumและcheckเข้ามาโดยใช้mapและsplit
Step2:กำหนดtime = [0] * 1441 ก็คือเป็นการสร้างตารางเลข0-1440เพื่อที่จะเช็๕ได้ว่าในเวลาไหนร้านเปิดอยู่กี่ร้าน
Step3:เข้าfor loop
Step4:รับค่าstart,end เข้ามาโดยใช้mapและsplit
Step5:คือการบันทึกสถานะของร้านค้าลงใน Listโดยให้เวลาเปิดstartมีค่า +1ก้คือร้านเปิดเพิ่มและเวลาปิด end มีค่า -1 ก้คือร้านลดลงเพื่อใช้คำนวณจำนวนร้านที่เปิดอยู่รวมทั้งหมดในภายหลัง
Step6:ตั้งcurrent_open = 0
Step7:เข้าfor loop เป็นการเช็คว่าเลขที่เราเคย+1,-1ไว้ เพื่ออัปเดตยอดรวม
Step8:ตั้ง check_times = list(map(int, input().split())) เพื่อเป็นการเช็คค่าที่เราต้องการเช็ค
Step9:ให้answersเป็นลิสต์ว่าง
Step10:เข้าfor loop
Step11:t = check_times[i] เป็นการเช็คทีละตัว
Step12:ใส่เพิ่มเข้าไปในlist
Step13:print *answers ต้องใส่ดอกจันทร์ด้วยไม่งั้นจะติดกล่องlist
```

---

## 4. วิธีสุดท้ายที่ใช้จริง

อธิบายสั้น ๆ ว่า algorithm หรือวิธีสุดท้ายที่ใช้จริงใน code ที่ส่งคืออะไร

หัวข้อนี้ต่างจาก Section 3:

- Section 3 คือแผนแรกก่อนใช้ AI ก่อนรับความช่วยเหลือจากคน หรือก่อนเขียน code สุดท้าย
- Section 4 คือวิธีสุดท้ายที่ใช้ใน solution จริง
- ถ้าวิธีสุดท้ายเหมือนกับแผนแรก ให้เขียนว่าเหมือนกัน และอธิบายสั้น ๆ ว่าทำไม

ห้ามคัดลอกคำอธิบายจาก AI

ห้ามคัดลอกคำอธิบายจากคนอื่น

```text
วิธีสุดท้ายเหมือนกับแผนแรกคือจะรับค่าnumและcheckเข้ามาโดยใช้mapและsplitจากนั้นก็กำหนดtime = [0] * 1441 ก็คือเป็นการสร้างตารางเลข0-1440เพื่อที่จะเช็คได้ว่าในเวลาไหนร้านเปิดอยู่กี่ร้านแล้วก็เข้าfor loop แล้วก็รับค่ารับค่าstart,end เข้ามาโดยใช้mapและsplitจกานั้นก็สร้างtime[start] = time[start] + 1 , time[end] = time[end] -1 มันก็คือการบันทึกสถานะของร้านค้าลงใน Listโดยให้เวลาเปิดstartมีค่า +1ก้คือร้านเปิดเพิ่มและเวลาปิด end มีค่า -1 ก้คือร้านลดลงเพื่อใช้คำนวณจำนวนร้านที่เปิดอยู่รวมทั้งหมดในภายหลังจากนั้นก็ตั้งcurrent_open = 0แล้วก็เข้าfor loop เป็นการเช็คว่าเลขที่เราเคย+1,-1ไว้ เพื่ออัปเดตยอดรวมตั้งแล้วก็ตั้งcheck_times = list(map(int, input().split())) เพื่อเป็นการเช็คค่าที่เราต้องการเช็คจกานั้นให้answersเป็นลิสต์ว่างแล้วเข้าfor loopอีกรอบเพื่อที่จะเช็คทีละตัว:t = check_times[i]ใช้.appendเพื่อใส่เพิ่มเข้าไปในlistและprint *answers ต้องใส่ดอกจันทร์ด้วยไม่งั้นจะติดกล่องlist
```

---

## 5. การทดสอบของฉัน

เขียน test cases อย่างน้อย 3 กรณีที่ลองเองหรือออกแบบเอง

พยายามเลือก test cases ที่แตกต่างกัน

แต่ละ test case ให้อธิบายว่าทำไมเลือกกรณีนั้น

ถ้า input หรือ output มีหลายบรรทัด ให้เขียนไว้ใน text blocks

### Test Case 1

ทำไมเลือก case นี้:

```text
ทดสอบการทำงานพื้นฐานว่าถ้าร้านมีช่วงเวลาเปิดทับซ้อนกัน โค้ดสามารถบวกสะสมจำนวนร้านได้ถูกต้องหรือไม่
```

Input:

```text
3 3
10 20
15 30
25 40
10 18 35
```

Expected output:

```text
1 2 1
```

Actual output:

```text
1 2 1
```

Result:

```text
Pass
```

### Test Case 2

ทำไมเลือก case นี้:

```text
ทดสอบการทำงานที่ขอบเขตเวลาสุดโต่งแบบนาทีที่ 0 และนาทีที่ 1440เพื่อเช็คว่าโค้ดสามารถรองรับค่าต่ำสุดและสูงสุดได้โดยไม่เกิด Error 
```

Input:

```text
2 4
0 720
720 1440
0 719 720 1440
```

Expected output:

```text
1 1 1 0
```

Actual output:

```text
1 1 1 0
```

Result:

```text
Pass
```

### Test Case 3

ทำไมเลือก case นี้:

```text
ทดสอบจุดบอดเรื่องเวลาปิดเพื่อยืนยันว่าถ้าร้านระบุเวลาปิดที่ 200 แล้วเราไปตรวจสอบ ณ เวลา 200 พอดีโค้ดจะต้องนับว่าร้าน ปิดแล้ว ไม่ใช่กำลังเปิด
```

Input:

```text
2 3
100 200
100 200
99 100 200
```

Expected output:

```text
0 2 0
```

Actual output:

```text
0 2 0
```

Result:

```text
Pass
```

---

## 6. การใช้ AI

ใช้ AI กับโจทย์นี้หรือไม่

```text
Yes
```

ถ้าใช้ AI ต้องทำไฟล์นี้ด้วย:

```text
ai_reflection.md
```

ถ้าถามเฉพาะเพื่อน TA หรือผู้สอน และไม่ได้ใช้ AI ไม่ต้องทำ `ai_reflection.md`

---

## 7. ความช่วยเหลือจากคน / การร่วมมือ

ได้ถามเพื่อน TA ผู้สอน หรือบุคคลอื่นเพื่อขอความช่วยเหลือในโจทย์นี้หรือไม่

```text
No
```

ถ้าใช่ ให้อธิบายสั้น ๆ ว่าได้รับความช่วยเหลือแบบใด

ตัวอย่างที่อนุญาต:

- อธิบายความหมายของโจทย์
- อธิบาย concept การเขียนโปรแกรม
- ให้ hint เกี่ยวกับแนวทาง
- คุยเรื่อง debugging
- คุยเรื่อง test cases
- ช่วยอธิบาย error message

สิ่งที่ไม่อนุญาต:

- คัดลอก code ของผู้อื่น
- ส่ง solution ของผู้อื่น
- ขอให้ผู้อื่นเขียน solution ให้
- ใช้ OJ submission ของผู้อื่น
- ขอให้ผู้อื่นส่ง OJ แทน

ใครช่วยคุณ

```text

```

เขาช่วยอะไร

```text

```

คุณยังทำอะไรด้วยตนเอง

```text

```

คุณคัดลอก code จากคนอื่นหรือไม่

```text
No
```

---

## 8. คำรับรองของนักศึกษา

เขียน `Yes` ในแต่ละ statement

| Statement | Yes/No |
|---|---|
| I wrote this submission in my own words. | Yes |
| I understand my final code. | Yes |
| I recorded the real OJ status. | Yes |
| I did not copy AI-generated text directly into this file. | Yes |
| I did not copy code from another person. | Yes |
| If I received human help, I disclosed it in this file. | Yes |
| I submitted the final code to the OJ by myself. | Yes |
